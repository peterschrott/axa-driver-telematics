package com.peedeex21.axa.algorithm

import com.peedeex21.axa.model.DriveMeta
import org.apache.flink.api.scala.DataSet

/**
 * Created by peter on 23.06.15.
 */
class OneVsRestSVM {

  def dosth(input: DataSet[DriveMeta]): Unit = {

    //val uniqueDriverIds = input.map(_.driverId).distinct(0)

  }
  

}
